import React from 'react';
import Link from 'react-router-dom'
const MovieCard = (props) => {
render(){
  
}
  return(
  
     <ul>
     <<li>
       <Link to=`/movies/${movies.id}/`>{movie.title}</Link> </li>
    </ul>
  )
  
}

export default MovieCard;
